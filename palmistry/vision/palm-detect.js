export async function detectPalm(image) {
  // Placeholder — real model connects here
  return {
    shape: "Earth",
    fingerType: "Balanced",
    mounts: ["Jupiter", "Apollo"],
    confidence: 0.82
  };
}
